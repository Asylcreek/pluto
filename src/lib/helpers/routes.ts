const home = () => '/';

const contactUs = () => '/contact-us';

const faqs = () => '/faqs';

const tickets = () => '/tickets';

const about = () => '/about';

const routes = {
  home,
  contactUs,
  faqs,
  tickets,
  about
};

export default routes;
