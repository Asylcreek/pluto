const home = () => '/';

const contactUs = () => '/contact-us';

const faqs = () => '/faqs';

const tickets = () => '/tickets';

const routes = {
  home,
  contactUs,
  faqs,
  tickets
};

export default routes;
